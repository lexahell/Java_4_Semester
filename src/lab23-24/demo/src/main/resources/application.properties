spring.application.name=demo
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always
management.endpoint.shutdown.enabled=true
endpoints.shutdown.enabled=true

student.name=Aleksey
student.last_name=Grigorev
student.group=IKBO-16-22

spring.datasource.url=jdbc:postgresql://localhost:5432/JavaTask
spring.datasource.username=postgres
spring.datasource.password=admin

spring.session.store-type=jdbc

spring.session.timeout=5m
spring.session.jdbc.initialize-schema=always
spring.session.jdbc.schema=classpath:schemas/schema-@@platform@@.sql
spring.session.jdbc.table-name=SPRING_SESSION

spring.jpa.hibernate.ddl-auto=update